/*#include <iostream>
#include <string>
#include <cmath>
using namespace std;
int main () {
	/*int N; // for 38
  cout << " N : ";
	cin >> N;
  float summ, slg;
	summ = 0;
	for (int i = 1; i <= N; i++) 
  {
		slg = pow(i, N - i + 1);
		summ += slg;
	}
	cout << summ;
}


int A, B; // FOR 39
  cout << " A : ";
  cin >> A;
  cout << " B : ";
  cin >> B;
  
	for (int x = A; x <= B; x++) {
	for (int y = 0; y < x; y++) {
	cout << x << " " << endl;
	}
	}
  }



	int N, i , out; // while 22
  out = 0;
  i = 2;
  cout << " N : ";
	cin >> N;
	while (i < N) {
	if (N % i == 0) { out = 1; }
	i++;
	}
	if (out == 1) { cout << "FALSE"; }
	else { cout << "TRUE"; }
}


	int A, B, out, k, nod,i; // while 23
	cout << " A : " ;
  cin >> A;
  cout << " B : " ;
  cin >> B ;
	nod = 0;
	out = 0;
	k = A % B;
	i = 2;
	while (i<B) {
		if (B % i == 0 and k % i == 0) { nod = i; }
		out = nod;
		i++;
	}
	cout << out;
  }

  


int A, B;  // while 23
  cout << " A : ";
  cin >> A;
  cout << " B : ";
  cin >> B;
  while ( A > 0 && B > 0)
    { if ( A > B ) 
      A = A % B ;
      else B = B % A;
    }
  cout << A + B << endl;
  }*/