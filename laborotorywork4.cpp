/*#include <iostream>
#include <cmath>
using namespace std;
int main () {
 /* int A, B ; // for 3
  cout << " A : " ;
  cin >> A;
  cout << " B : " ;
  cin >> B;
  for ( int i = B - 1; i > A ; i--)
  cout << i << " ";
}


int N ;
float sum = 0, slg; // for 10
cout << " N : " ;
cin >> N;
for ( float i = 1 ; i <= N ; i++)
{ slg = 1 / i;
sum = sum + slg; }
cout << sum;
}

int N, a = 1; // for 15
cout << " N : " ;
cin >> N ;
float A ;
cout << " A : " ;
cin >> A ;
for ( int i = 1 ; i <= N ; i++) {
 a = a * A ;
}
cout << a << endl;
}

float X; // while 24
  cout << " X : " ;
  cin >> X;
int N;  
  cout << " N : " ;
  cin >> N;
float sum = 0 , f = 1, an = 0;
  for ( int i = 1; i <= N; i++ ) {
		f = f * i * (i + 1);
		an = pow(-1, i) * pow(X, 2 * N) / f;
		sum = sum + an;
	}
	cout << sum;
  }

/*int n; // while 33
	float f1, f2, f3, sum;
	cin >> n;
	f1 = 1;
	f2 = 1;
	sum = 0;
	cout << "1 1 ";
	for (int i = 0; i < n;i++) {
		f3 = f2 + f1;
		cout << f3 << " ";
		sum = f2 + f3;
		f2 = f3;
		f1 = sum - f3;
	}
}*/

