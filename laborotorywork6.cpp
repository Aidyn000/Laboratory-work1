/*#include <iostream>
#include <string>
#include <cmath>
using namespace std;
int main() {
 /* int N, A, D , An = 0; // Array 3
  cout << " N = ";
  cin >> N;
  cout << " A = ";
  cin >> A;
  cout << " D = ";
  cin >> D;
  int arr[N];
  for (int i=0; i < N; i++ )
    { An = A + i * D; 
      An = arr[i];
  cout << "An=" << arr[i] << endl;
      }
  }

 /* int A[10];
int N;
cout << " N : ";
  cin >> N ;
  for (int i = 0 ; i < N ; i ++)
    { cout << i << A[i];
      }
  for (int i = 1 ; i < N ; i += 2)
    { cout << i <> A[i];
      }
  for ( int i = 0 ; i < N ; i += 2)
    { cout << i << endl;} 
  }

*/
 /*int a[10];  // array 22
    int N , K , L;
 cin >> N >> K >> L;
    int i;
    for (i=0; i<N; ++i){
     printf("a[%i] : ",i);
        scanf("%i",&a[i]);
    }
 
    int sum=0;
    for (i=0; i<K-1; ++i) sum+=a[i];
    for (i=L; i<N; ++i) sum+=a[i];
    printf("%i\n: ",sum);
    return 0;
}
 
int a[10]; // array 14
    int n, i;
 
    printf("N: ");
    scanf("%i",&n);
 
    for (i=0; i<n; ++i){
        printf("a[%i] : ",i+1);
        scanf("%i",&a[i]);
    }
 
    for (i=1; i<n; i+=2)
            printf("%i\n",a[i]);
 
    for (i=0; i<n; i+=2)
            printf("%i\n",a[i]);
 
    return 0;
}

int a[10]; // array 28
    int n, i;
 
    printf("N: ");
    scanf("%i",&n);
 
    for (i=0; i<n; ++i){
        printf("a[%i] : ",i+1);
        scanf("%i",&a[i]);
    }
 
    for (i=1; i<n; i+=2)
            printf("%i\n",a[i]);
 
    for (i=0; i<n; i+=2)
            printf("%i\n",a[i]);
 
    return 0;
}
 int a[10]; // 
    int n;
 
    printf("N: ");
    scanf("%i",&n);
 
    int i;
    for (i=0; i<n; ++i){
        printf("a[%i] : ",i+1);
        scanf("%i",&a[i]);
    }
 
    int min = a[1];
 
    for (i=1; i<n; i+=2) {
        if (a[i]<min) min=a[i];
    }
 
    printf("%i\n",min);
 
    return 0;
}*/
 
