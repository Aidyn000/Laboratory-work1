/*#include <iostream>
#include <string>
#include <cmath>
using namespace std;
int main() {
 /* int a,i,n;
  cin>>n>>a;
  double sum,k;
  sum=0;
  i=1;
  while(i<=n){ 
  k= a+cos(a*i)/2;
  sum += k;
i++;
  }
  cout<<sum;
  }
*/
/*int a,i,n;
  cin>>n>>a;
  double sum,k;
  sum=0;
  for (i = 1 ; i<=n ; i++){
  k= a+cos(a*i)/2;
  sum += k;
  }
  cout<<sum;
  }


int a,i,n;
  cin>>n>>a;
  double sum,k;
  sum = 0;
  i = 1;
  do
  {
  k= a+cos(a*i)/2;
  sum += k;
    i++;
    }
  while(i<=n); 
  cout << sum;
  }*/